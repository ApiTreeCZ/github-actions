name: Parse secrets
description: Parses a JSON secrets string and exposes each secret as an output variable

inputs:
  json:
    description: 'JSON string of secrets to parse'
    required: true

runs:
  using: 'node24'
  main: 'index.js'
