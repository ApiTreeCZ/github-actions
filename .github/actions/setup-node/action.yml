name: Set up Node.js
description: Set up Node.js with pnpm for CI

runs:
  using: composite
  steps:
    - name: Set up pnpm
      uses: pnpm/action-setup@v6

    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        cache: pnpm
        node-version-file: 'package.json'
